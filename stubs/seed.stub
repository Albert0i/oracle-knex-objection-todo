/**
 * Example seed stub
 *
 * @param { import("knex").Knex } knex - The Knex instance connected to your DB
 * @returns { Promise<void> }
 */
export async function seed(knex) {
  // STEP 1: Clear existing data
  // -----------------------------------
  // Typically you start by deleting rows from the target table
  // so that seeding always produces a consistent dataset.
  // Use caution: this will wipe ALL rows in the table.
  //
  // Example:
  // await knex('SAMPLE_TABLE').del();

  // STEP 2: Define seed data
  // -----------------------------------
  // Create an array of objects representing rows you want to insert.
  // Each object should match the column names of your table.
  //
  // Example:
  // const sampleData = [
  //   { name: 'Alice', status: 'ACTIVE' },
  //   { name: 'Bob', status: 'INACTIVE' },
  //   { name: 'Charlie', status: 'ACTIVE' }
  // ];

  // STEP 3: Insert seed data
  // -----------------------------------
  // Insert the array into the table. For Oracle, you may need to
  // insert rows one by one instead of batch insert.
  //
  // Example:
  // for (const row of sampleData) {
  //   await knex('SAMPLE_TABLE').insert(row);
  // }

  // NOTE:
  // - Keep seed data small and simple (for testing/dev environments).
  // - Use realistic values if you want to simulate production scenarios.
  // - Seeds are optional; you can skip them if you only need migrations.
}

/*
   Knex.js Seeds
   https://knexjs.org/guide/migrations.html#seed-cli
*/
